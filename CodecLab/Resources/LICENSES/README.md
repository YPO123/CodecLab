# Third-Party License Notices

Release builds that bundle FFmpeg must include:

- FFmpeg version
- FFmpeg configure flags
- FFmpeg license
- License notices for enabled codec libraries such as `libmp3lame` and `libopus`

This prototype does not bundle FFmpeg binaries.

